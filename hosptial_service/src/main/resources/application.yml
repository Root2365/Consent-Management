server:
  port: 9005
credentials:
  id: "1"
  name: "Mera-hospital"
  password: "abc"
hospital-manager:
  address: "http://localhost:9001/"
spring:
  application:
    name: hospital-service

  datasource:
    url: jdbc:mysql://localhost:3306/hospital_one
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

eureka:
  client:
    enabled: false